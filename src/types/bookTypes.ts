export interface BookTypes {
    title: string;
    author: string;
    description: string;
    cover: string;
    price: string;
    id: number;
}

export interface SortBookTypes {
    title: string;
    booksCount: number;
    price: string;
}